name: ZIVPN Native Build
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  BuildAPK:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Fix Submodule Path
        run: |
          mkdir -p core/src/foss/golang/clash
          git submodule update --init --recursive --force
          cp -r core/src/foss/golang/clash/* core/src/foss/golang/clash/ || true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: 17

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: Update CA Certificates
        run: |
          sudo apt-get update && sudo apt-get install -y ca-certificates
          sudo update-ca-certificates

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build APK (Alpha Release)
        run: |
          chmod +x gradlew
          export JAVA_HOME=$JAVA_HOME_21_X64
          ./gradlew :app:assembleAlphaRelease --stacktrace

      - name: Upload ZIVPN Native APK
        uses: actions/upload-artifact@v4
        with:
          name: ZIVPN-Native-APK
          path: app/build/outputs/apk/alpha/release/*.apk